<?xml version="1.0" encoding="utf-8"?>
<!--
    Outline-only pill background for dialog secondary/cancel (left) buttons.
    No solid fill — transparent with brown stroke for visibility against
    white dialog background. Brown text set separately in Button.Subtle style.
    Applied via Widget.ZejiosCafe.Dialog.Button.Subtle.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="14dp" />
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="2dp"
                android:color="@color/pos_primary" />
        </shape>
    </item>
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <corners android:radius="14dp" />
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="2dp"
                android:color="@color/pos_primary" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="14dp" />
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="2dp"
                android:color="@color/pos_primary" />
        </shape>
    </item>
</selector>
